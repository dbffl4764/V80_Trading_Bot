name: V80_Trading_Bot

on:
  schedule:
    - cron: '*/30 * * * *' # 30분마다 더 자주 시도해서 뚫릴 확률 높임
  workflow_dispatch:

jobs:
  trading:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install ccxt pandas python-dotenv

      - name: Run Bot
        env:
          BINANCE_API_KEY: ${{ secrets.BINANCE_API_KEY }}
          BINANCE_SECRET_KEY: ${{ secrets.BINANCE_SECRET_KEY }}
          # 💡 깃허브가 제공하는 다양한 IP 대역을 활용하도록 수동 실행 반복 시도 권장
        run: python main.py
